defaults:
  - model: models
  - data: hemoset
  - trainer: default
  - _self_

models_to_bench: ["unet", "unet_plus_plus", "swin_unet"]
seed: 42
device: cuda
debug: false

logging:
  project: surgical-copilot
  wandb_enabled: true
